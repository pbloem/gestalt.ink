title:            gestalt.ink
description:      Bottom-up explanations

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

defaults:
  -
    scope:
        path: ""
    values:
        home: false     # whether we're on the home page
        class: ''       # Will be added to the class attributed of the body tag for styling
        summary: ''